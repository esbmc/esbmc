import math
import math

a = math.sin(0)
b = math.sin(0)
c = math.cos(0)  
d = math.cos(0)
e = math.sqrt(2)
f = math.sqrt(2)

assert a == b        # Both should be 0
assert c == d        # Both should be 1
assert e == f        # Both should be sqrt(2)
