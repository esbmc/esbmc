#include <assert.h>
int main()
{
  int num = 0;
  int a = 5 / num; // division by zero in line 3

  for (int i = 0; i <= 10; i++)
  {
    assert(1);
  }

  int num1 = 0;
  int b = 5 / num1; // division by zero in line 7
}