// Auto-generated harness for: strcpy
// Source: src/c2goto/library/string.c:27
// Generated by Byte Repair Contractor

#include <stddef.h>

extern char *strcpy(char *dst, const char *src);

void __byterepair_harness_main(void) {
    // --- Symbolic inputs ---
    char dbuf[8], sbuf[8];
    sbuf[7] = '\0';
    char *dst = dbuf;
    const char *src = (const char *)sbuf;

    // --- Call under test ---
    char *result = strcpy(dst, src);

    // --- Postcondition assertions ---
    __ESBMC_assert(result == dst, "system property: strcpy returns dst");

    // For every index i up to and including the first '\0' in src,
    // dst[i] == src[i]. Expressed as forall: skip indices past the
    // terminator (src[i-1] == '\0' && i > 0).
    size_t i;
    __ESBMC_assert(
        __ESBMC_forall(&i,
            !(i < 8) || !(i == 0 || src[i - 1] != '\0') || dst[i] == src[i]),
        "system property: strcpy copies every byte up to and including the null terminator");
}
