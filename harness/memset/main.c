// Auto-generated harness for: memset
// Source: src/c2goto/library/string.c:303
// Generated by Byte Repair Contractor

#include <stddef.h>

extern void *memset(void *s, int c, size_t n);

void __byterepair_harness_main(void) {
    // --- Symbolic inputs ---
    size_t n = nondet_uint();
    __ESBMC_assume(n <= 8);
    unsigned char buf[8];
    void *s = (void *)buf;
    int c = nondet_int();

    // --- Precondition assumptions ---
    // memset's contract requires s != NULL. The 8-byte stack buffer plus
    // n <= 8 keeps the body's writes in-bounds (the buffer-size UB is
    // bounded here rather than in the contract because
    // __ESBMC_get_object_size in __ESBMC_requires is mishandled by ESBMC's
    // --replace-all-contracts pipeline; see report).

    // --- Call under test ---
    void *result = memset(s, c, n);

    // --- Postcondition assertions ---
    size_t i = nondet_uint();
    __ESBMC_assume(i < n);
    __ESBMC_assert(result == s, "system property: memset returns s");
    __ESBMC_assert(((unsigned char *)s)[i] == (unsigned char)c,
        "system property: memset writes c into every byte of [s, s+n)");
}
