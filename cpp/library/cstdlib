#ifndef STL_STDLIB
#define STL_STDLIB

//#include <stdlib.h>

unsigned int nondet_uint();

#define RAND_MAX        2147483647

//int seed, last_value=-1;

int rand(void);
void srand (unsigned int s);

int rand(void)
{
#if 0
  if (seed==1 && last_value!=-1) {
    return last_value;
  }  else {
    last_value = nondet_uint() % RAND_MAX;
    return last_value;
  }
#endif

    return nondet_uint() % RAND_MAX;
}

#if 0
void srand (unsigned int s)
{
  seed = s;    
}
#endif

#endif
