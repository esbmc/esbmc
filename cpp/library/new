#ifndef STL_NEW
#define STL_NEW

#include "definitions.h"

namespace std {

	typedef void (*new_handler)();

	new_handler set_new_handler (new_handler new_p) throw();

 
   char* bad_alloc(const char *_Message);
   
}

#endif
