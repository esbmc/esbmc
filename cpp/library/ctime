#ifndef STL_CTIME
#define STL_CTIME

#include "definitions"

typedef unsigned int time_t;

time_t time (time_t *timer);

time_t time (time_t *timer)
{
  time_t t = nondet_uint();

  if (timer!=NULL)
    *timer = t;

  return t;
}

#endif

