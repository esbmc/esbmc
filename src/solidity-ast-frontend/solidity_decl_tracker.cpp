#include <solidity-ast-frontend/solidity_decl_tracker.h>

namespace ConfigureTracker
{
  void configre_decl_function_tracker(const nlohmann::json& decl, DeclTrackerPtr& json_tracker)
  {
    assert(!"configuring ...");
  }
};
